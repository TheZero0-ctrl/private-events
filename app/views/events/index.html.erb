<% if !@events.upcoming.empty? %>
  <%= render "events/list_of_events", events: @events.upcoming, head: "Upcomming Events" %>
<% end %>

<% if !@events.past.empty? %>
   <%= render "events/list_of_events", events: @events.past, head: "Past Events" %>
<% end %>

